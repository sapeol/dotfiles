# Git
alias gs="git st"
alias gl="git lg"
alias gb="git br"
alias lg="lazygit"
alias tg="tig"

# Better defaults as drop-in replacements
# Use \cat, \ls, or \grep to bypass and use original
alias cat='bat'
alias ls='eza'
alias grep='rg'
